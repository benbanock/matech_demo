<%= simple_form_for([item, tag], remote: true) do |f| %>
  <%= f.text_field :name, placeholder: "Add a tag", label: false, style: "height: 39px" %>
  <%= f.submit "Add", :class => "create btn btn-default"  %>
<% end %>

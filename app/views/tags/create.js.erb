function addTag(newTagHTML) {
  const tagContainer = document.querySelector(".tag-index");
  tagContainer.insertAdjacentHTML("beforeend", newTagHTML);
}

addTag("<%= j render 'items/tag', item: @item, tag: @tag %>");
